# Segredos (credenciais do Neon; .env.local que o CLI da Vercel gera) — NUNCA commitar
.env*

# Base de dados gerada
*.db

# Capturas de descoberta (grandes, regeneráveis)
capturas_*.json

# Mídia baixada do Instagram (flyers p/ extração/auditoria; URLs do CDN expiram,
# mas a extração já persistida na base torna os arquivos regeneráveis-o-bastante)
midias/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Jupyter
.ipynb_checkpoints/

# Estado local de ferramentas (Codex cria config de projeto aqui)
.codex/

# Estado local do CLI da Vercel (link do projeto)
.vercel

# Sistema
.DS_Store
Thumbs.db

# Front (Next.js) — build e dependências
node_modules/
.next/
next-env.d.ts
