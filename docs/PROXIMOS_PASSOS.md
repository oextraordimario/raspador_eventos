# Próximos passos — MOVIDO

> **Este arquivo foi absorvido pelo backlog estruturado em [`docs/backlogs/`](backlogs/)**
> (2026-07-09). O git preserva o histórico deste arquivo.

Para onde foi cada item:

- **1. Qualidade das respostas do agente** → `NI-08` em
  [`backlogs/nao-iniciado.yaml`](backlogs/nao-iniciado.yaml).
- **2. Apertar o filtro** → **feito na Fase 0** (filtro de ruído v1 — ver
  [`specs/20260709_mvp-fase-0/execucao.md`](specs/20260709_mvp-fase-0/execucao.md);
  termos descartados em [`backlogs/rejeitado.yaml`](backlogs/rejeitado.yaml));
  **classificar gênero** → `NI-05` (LLM v2).
- **3. Cobertura** (catálogo completo) e **dedupe cross-fonte** → **feitos na Fase 0**
  (mesma spec); **re-raspagem agendada** → `NI-10` (GitHub Actions, Fase 1).
- **4. Postgres (Neon)** → `NI-09`.
- **Qualidade de dados observada** → registrada nas convenções do `CLAUDE.md` e nos
  itens `NI-01` (dedupe intra-fonte) e `NI-04` (ruído v2).
